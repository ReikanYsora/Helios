[project]
name = "helios-python-tools"
version = "0.1.0"
description = "Python helper tools for Helios local nDSM workflows"
requires-python = ">=3.12,<3.13"
dependencies = [
    "numpy>=2.0,<3",
    "gdal>=3.8,<4",
]

[tool.uv]
package = false
